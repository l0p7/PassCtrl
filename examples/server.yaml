server:
  listen:
    address: "0.0.0.0"
    port: 8080
  logging:
    level: info
    format: json
    correlationHeader: "X-Request-ID"
  rules:
    rulesFolder: "/rules"
  templates:
    templatesFolder: "/templates"

# Define endpoints and rules as needed by your deployment.

